"""Domain models and identifier helpers for SEC RAG.

Pure data and pure functions only. No IO, no network, no database. Other layers
(storage, edgar, index) depend on these types; these types depend on nothing in
the project.
"""
